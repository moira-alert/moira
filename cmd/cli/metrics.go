package main

import (
	"github.com/moira-alert/moira"
	"time"
)

func cleanupOutdatedMetrics(config cleanupMetricsConfig, database moira.Database, duration time.Duration, logger moira.Logger) error {
	return nil
}

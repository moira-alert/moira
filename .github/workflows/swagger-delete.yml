name: Delete spec version from SwaggerHub
defaults:
  run:
    working-directory: .
on:
  pull_request:
    types: [closed]
    branches:
    - master
  push:
    branches:
    - feature/*
    - release/[0-9]+.[0-9]+.[0-9]+
    - master

jobs:
  removespec:
    name: Delete api from SwaggerHub
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: '16.16.0'
      - run: npm i --location=global swaggerhub-cli
      - run: |
          SWAGGERHUB_API_KEY=${{secrets.SWAGGERHUB_TOKEN}} swaggerhub api:unpublish "Moira-test-swagger/moira-alert-test/latest"
          SWAGGERHUB_API_KEY=${{secrets.SWAGGERHUB_TOKEN}} swaggerhub api:delete    "Moira-test-swagger/moira-alert-test/latest"